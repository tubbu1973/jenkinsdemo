pipeline {
    agent any 

    stages {
        stage('Build') {
            steps {
                // Installs the npm dependencies for the project
                bat 'npm install' 
            }
        }
    }
}
